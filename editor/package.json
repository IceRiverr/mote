{
  "name": "@mote/editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:schemas": "cd ../packages/engine && npx tsx scripts/extract-schemas.ts",
    "predev": "npm run build:schemas",
    "prebuild": "npm run build:schemas",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.22.0",
    "@preact/signals": "^1.3.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
